package servicedelivery;

import org.osgi.framework.BundleActivator;
import org.osgi.framework.BundleContext;
import org.osgi.framework.ServiceRegistration;

public class Activator implements BundleActivator {

	ServiceRegistration serviceRegistration;
	/*
	 * (non-Javadoc)
	 * @see org.osgi.framework.BundleActivator#start(org.osgi.framework.BundleContext)
	 */
	public void start(BundleContext bundleContext) throws Exception {
		System.out.println("delivery service start");
        final ServiceDelivery serviceDelivery = new ServiceDeliveryImp();
        this.serviceRegistration = bundleContext.registerService(ServiceDelivery.class.getName(), (Object)serviceDelivery, null);
	}

	/*
	 * (non-Javadoc)
	 * @see org.osgi.framework.BundleActivator#stop(org.osgi.framework.BundleContext)
	 */
	public void stop(BundleContext bundleContext) throws Exception {
		 System.out.println("delivery service stop");
	        this.serviceRegistration.unregister();
	}

}
